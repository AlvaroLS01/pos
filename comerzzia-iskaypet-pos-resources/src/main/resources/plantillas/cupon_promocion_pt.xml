<plantilla>
    <documento>

        #set( $imprimirDatosFacturacion = false )
        #parse("./plantillas/datos_cabecera_cupon_pt.xml")

        <linea line-size = "1">
            <texto align="center" size="41">${cupon.getTituloCupon()}</texto>
        </linea>

        <linea><texto></texto></linea>

        <linea>
            <texto align="left" size="14">Data: </texto>
            <texto align="right" size="27" style="1">${fechaEmision}</texto>
        </linea>

        <linea>
            <texto align="left" size="20">Foi atendido por:</texto>
            <texto align="right" size="21">${ticket.getCabecera().getCajero().getDesusuario()}</texto>
        </linea>

        #if(${ticket.getCabecera().getDatosFidelizado()} && ${ticket.getCabecera().getDatosFidelizado().getNumTarjetaFidelizado()})
        <linea>
            <texto align="left" size="20">Cliente:</texto>
            <texto align="right" size="21">${ticket.getCabecera().getDatosFidelizado().getNumTarjetaFidelizado()}</texto>
        </linea>

        #end

        <linea><texto size="41" align="center">-----------------------------------------</texto></linea>

        ## DIVISA
        #set($divisa = "")
        #if(${ticket.getCabecera().getDivisa()})
        #if(${ticket.getCabecera().getDivisa().equals("EUR")})
        #set($divisa = "€")
        #end
        #end

        #if(${cupon.getImporteCupon()} > 0)
        <linea line-size = "1">
            <texto align="center" size="41">${importeCuponFormateado}$divisa</texto>
        </linea>
        <linea><texto> </texto></linea>
        #end

        #foreach($lineaDescripcion in $textUtils.divideLines(${cupon.getDescripcionCupon()}, 41))
        <linea>
            <texto size="41" align="center">${lineaDescripcion}</texto>
        </linea>
        #end

        <linea><texto size="41" align="center">-----------------------------------------</texto></linea>
        <!-- tenemos en cuenta las fechas de inicio y fin de activación -->
        #if(${fechaInicio}!="")
        <linea>
            <texto align="left" size="20">Válido desde:</texto>
            <texto align="right" size="21" style="1">${fechaInicio}</texto>
        </linea>
        #end
        #if(${fechaFin}!="")
        <linea>
            <texto align="left" size="20">Válido até:</texto>
            <texto align="right" size="21" style="1">${fechaFin}</texto>
        </linea>
        #end
        #if(${maximoUsos}!="")
        <linea>
            <texto align="left" size="23">Número máximo de usos:</texto>
            <texto align="right" size="18" style="1">${maximoUsos}</texto>
        </linea>
        #end

        #if((${cupon.getIdPromocionOrigen()} && ${cupon.getIdPromocionOrigen()} != 0 ) ||
            (${cupon.getIdPromocionAplicacion()} && ${cupon.getIdPromocionAplicacion()} != 0))
            <linea><texto> </texto></linea>
            #if(${cupon.getIdPromocionOrigen()} && ${cupon.getIdPromocionOrigen()} != 0 )
                <linea>
                    <texto size="26" align="left" fontsize="4" style="1">Promoção emissora:</texto>
                    <texto size="28" align="right" fontsize="4" style="1">${cupon.getIdPromocionOrigen()}</texto>
                </linea>
            #end
            #if(${cupon.getIdPromocionAplicacion()} && ${cupon.getIdPromocionAplicacion()} != 0)
                <linea>
                    <texto size="26" align="left" fontsize="4" style="1">Promoção receptora:</texto>
                    <texto size="28" align="right" fontsize="4" style="1">${cupon.getIdPromocionAplicacion()}</texto>
                </linea>
            #end
        #end

        ## DIVISA
        #set($divisa = "")
        #if(${ticket.getCabecera().getDivisa()})
            #if(${ticket.getCabecera().getDivisa().equals("EUR")})
                #set($divisa = "€")
            #end
        #end

        #if(${cupon.getImporteCupon()} > 0)
        <linea><texto size="41" align="center">-----------------------------------------</texto></linea>
        <linea line-size = "1">
            <texto align="center" size="41">${importeCuponFormateado}$divisa</texto>
        </linea>
        #end

        <linea><texto size="41" align="center">-----------------------------------------</texto></linea>

        <linea>
            <codbar align="center" tipo-leyenda="2" size="41">${cupon.getCodigoCupon()}</codbar>
            <!-- <codbar type="QR" align="center">${cupon.getCodigoCupon()}</codbar>-->
        </linea>
        <linea line-size = "1">
            <texto size="41" align="center">CUPÃO NÃO ACUMULÁVEL</texto>
        </linea>
        #if(${exclusiones} && ${exclusiones}!="")
        <linea><texto> </texto></linea>
        <linea>
            <texto align="center" size="40">${exclusiones}</texto>
        </linea>
        #end
        <linea><texto> </texto></linea>
        <linea>
            <texto size="41" align="center" style="1">Obrigado pela visita</texto>
        </linea>

        #if($imprimirLogo)
            <linea><texto></texto></linea>
            <linea><texto></texto></linea>
            <linea><texto></texto></linea>
        #end

        <corte></corte>
    </documento>
</plantilla>