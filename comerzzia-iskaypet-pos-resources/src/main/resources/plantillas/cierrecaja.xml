<plantilla>
    <documento>
		#if($imprimirLogo)
		    <logo alignLogo="center"></logo>
		#end

		#if(${caja.getCodAlmAsString()}&&${caja.getCodCajaAsString()})
			<linea>
				<texto size="21" align="left">CAJA:</texto>
				<texto size="20" align="right">${caja.getCodAlmAsString()}/${caja.getCodCajaAsString()}</texto>
			</linea>
		#end

		#if(${fechaApertura})
			<linea>
				<texto size="21" align="left">FECHA APERTURA:</texto>
				<texto size="20" align="right">${fechaApertura}</texto>
			</linea>
		#end

		#if(${fechaCierre})
			<linea>
				<texto size="21" align="left">FECHA CIERRE:</texto>
				<texto size="20" align="right">${fechaCierre}</texto>
			</linea>
		#end

		#if(${usuario})
			<linea>
				<texto size="21" align="left">USUARIO:</texto>
				<texto size="20" align="right">${usuario.getDesusuario()}</texto>
			</linea>
		#end

		#if(${fechaImpresion})
			<linea>
				<texto size="21" align="left">FECHA IMPRESION:</texto>
				<texto size="20" align="right">${fechaImpresion}</texto>
			</linea>
		#end

		<linea><texto>----------------------------------------</texto></linea>

		<linea>
			<texto size="41" align="left">RESUMEN TOTALIZADOS</texto>
		</linea>
		
		<linea>
			<texto size="21" align="left">Total de tickets:</texto>
			<texto size="20" align="right">${caja.getTotalTicketsAsString()}</texto>
		</linea>   
		#if( ${caja.getTotalApuntesEntradaAsString()})
		<linea> 
			<texto size="34" align="left">A) MOV. ENTRADAS (Saldo inicial):</texto>
			<texto size="7" align="right">${saldoInicial}</texto>
		</linea>
		<linea> 
			<texto size="34" align="left">   MOV. ENTRADAS (Entrada Efec.):</texto>
			<texto size="7" align="right">${entradaEfectivo}</texto>
		</linea>
		#end
		#if(${caja.getTotalVentasEntradaAsString()})
		<linea>
			<texto size="21" align="left">B) VENTAS:</texto>
			<texto size="20" align="right">${caja.getTotalVentasEntradaAsString()}</texto>
		</linea>
		#end
		#if(${caja.getNumTicketsEntrada()})
		<linea>
			<texto size="21" align="left">   Tickets de ventas:</texto>
			<texto size="20" align="right">${caja.getNumTicketsEntrada()}</texto>
		</linea>
		#end
		
		#if( ${caja.getTotalEntradasAsString()})
		<linea> 
			<texto size="20" align="left">E = (A+B)</texto> 
			<texto size="20" align="right">${caja.getTotalEntradasAsString()}</texto>
		</linea>
		#end

		<linea><texto>----------------------------------------</texto></linea>

		#if(${caja.getTotalApuntesSalidaAsString()})
			<linea>
				<texto size="34" align="left">C) MOV. SALIDA (Retiradas):</texto>
				<texto size="7" align="right">${retiradas}</texto>
			</linea>
			<linea>
				<texto size="34" align="left">   MOV. SALIDA (Salida Caja):</texto>
				<texto size="7" align="right">${salidaCaja}</texto>
			</linea>
		#end
		#if(${caja.getTotalVentasSalidaAsString()})
			<linea>
				<texto size="21" align="left">D) DEVOLUCIONES:</texto>
				<texto size="20" align="right">${caja.getTotalVentasSalidaAsString()}</texto>
			</linea>
		#end
		#if(${caja.getNumTicketsSalida()})
		<linea>
			<texto size="21" align="left">   Tickets de devoluciones:</texto>
			<texto size="20" align="right">${caja.getNumTicketsSalida()}</texto>
		</linea>
		#end

		
		<linea> 
			<texto size="21" align="left">S = (C+D)</texto>
			<texto size="20" align="right">${caja.getTotalSalidasAsString()}</texto>
		</linea>
		
		<linea> 
			<texto size="21" align="left">(E-S)</texto>
			<texto size="20" align="right">${caja.getTotalAsString()}</texto>
		</linea>

		<linea><texto>----------------------------------------</texto></linea>
		
		<linea>
			<texto size="17" align="left">MEDIO PAGO</texto>
			<texto size="8" align="right">TOTAL</texto>
			<texto size="8" align="right">RECUENTO</texto>
			<texto size="8" align="right">DESC</texto>
		</linea>
		
		#foreach($acumulado in $acumulados)
		<linea>
			#if(${acumulado.getMedioPago().getDesMedioPago()})
			<texto size="17" align="right">${acumulado.getMedioPago().getDesMedioPago()}</texto>
			#end
			#if(${acumulado.getTotal()})
			<texto size="8" align="right">${acumulado.getTotalAsString()}</texto>
			#end
			#if(${acumulado.getRecuento()})
			<texto size="8" align="right">${acumulado.getRecuentoAsString()}</texto>
			#end
			#if(${acumulado.getDescuadre()})
			<texto size="8" align="right">${acumulado.getDescuadreAsString()}</texto>
			#end
		</linea>
		#end

		
		<linea>
			<texto size="41" align="center">TOTALES</texto>
		</linea>

		<linea>
			<texto size="21" align="right">TOTAL</texto>
			<texto size="8" align="right">DECLARADO</texto>
			<texto size="8" align="right">DESCUADRE</texto>
		</linea>
		
		<linea>
			#if(${caja.getTotalAsString()}) 
			<texto size="21" align="right">${caja.getTotalAsString()}</texto>
			#end
			#if(${caja.getTotalRecuentoAsString()})
			<texto size="13" align="right">${caja.getTotalRecuentoAsString()}</texto>
			#end
			#if(${caja.getDescuadreAsString()})
			<texto size="13" align="right">${caja.getDescuadreAsString()}</texto> 
			#end
		</linea>
		<linea><texto></texto></linea>
		#if($imprimirLogo)
			<linea><texto></texto></linea>
			<linea><texto></texto></linea>
			<linea><texto></texto></linea>
		#end
		<corte></corte>
	</documento>
</plantilla>
